import React,{useState}from'react';import{Link,useSearchParams}from'react-router-dom';import{KeyRound}from'lucide-react';import{resetPassword}from'../services/passwordResetService';
const ResetPassword=()=>{const[params]=useSearchParams();const[password,setPassword]=useState('');const[message,setMessage]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);const submit=async(e)=>{e.preventDefault();if(password.length<8)return setError('Password must have at least 8 characters.');setLoading(true);setError('');try{const result=await resetPassword(params.get('token'),password);setMessage(result.message);}catch(err){setError(err.response?.data?.message||'Unable to reset password.');}finally{setLoading(false);}};return <main className="min-h-screen bg-[#0B1A13] px-4 py-16 text-white"><form onSubmit={submit} className="mx-auto max-w-md rounded-3xl border border-white/10 bg-[#0F2119] p-8"><KeyRound className="mb-5 text-gold" size={30}/><h1 className="text-2xl font-bold">Choose a new password</h1>{message&&<p className="mt-5 rounded-xl bg-emerald-400/10 p-3 text-sm text-emerald-200">{message}</p>}{error&&<p className="mt-5 rounded-xl bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password (8+ characters)" className="mt-6 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none focus:border-gold/50"/><button disabled={loading||!params.get('token')} className="mt-4 w-full rounded-xl bg-gold px-4 py-3 font-bold text-darkGreen disabled:opacity-50">{loading?'Updating…':'Update password'}</button><Link className="mt-5 block text-center text-sm text-white/50 hover:text-gold" to="/">Return to login</Link></form></main>};export default ResetPassword;
