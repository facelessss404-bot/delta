import React, { useState } from 'react'; import { Link } from 'react-router-dom'; import { KeyRound } from 'lucide-react'; import { requestPasswordReset } from '../services/passwordResetService';
const ForgotPassword=()=>{const[email,setEmail]=useState('');const[message,setMessage]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);const submit=async(e)=>{e.preventDefault();setLoading(true);setError('');try{const result=await requestPasswordReset(email);setMessage(result.message);}catch(err){setError(err.response?.data?.message||'Unable to request a password reset.');}finally{setLoading(false);}};return <main className="min-h-screen bg-[#0B1A13] px-4 py-16 text-white"><form onSubmit={submit} className="mx-auto max-w-md rounded-3xl border border-white/10 bg-[#0F2119] p-8 shadow-2xl"><KeyRound className="mb-5 text-gold" size={30}/><h1 className="text-2xl font-bold">Reset your password</h1><p className="mt-2 text-sm text-white/50">Enter your account email. If it exists, we will send a reset link.</p>{message&&<p className="mt-5 rounded-xl bg-emerald-400/10 p-3 text-sm text-emerald-200">{message}</p>}{error&&<p className="mt-5 rounded-xl bg-red-400/10 p-3 text-sm text-red-200">{error}</p>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email address" className="mt-6 w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none focus:border-gold/50"/><button disabled={loading} className="mt-4 w-full rounded-xl bg-gold px-4 py-3 font-bold text-darkGreen disabled:opacity-50">{loading?'Sending…':'Send reset link'}</button><Link className="mt-5 block text-center text-sm text-white/50 hover:text-gold" to="/">Return to login</Link></form></main>};export default ForgotPassword;
