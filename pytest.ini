[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    smoke: non-destructive availability and authentication coverage
    e2e: browser, API, and PostgreSQL workflow coverage
    security: role and data-ownership authorization coverage
    destructive: creates or removes isolated-test-database data
